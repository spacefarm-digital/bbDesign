const modalButton = document.querySelector('#open-demo-modal');
const demoModal = document.getElementById('demo-modal');
const modalWrapper = document.getElementById('demo-modal-wrapper');

const dialog = new A11yDialog(demoModal, modalWrapper);